[project]
name = "xray-deploy"
version = "0.1.0"
description = "Deployment automation for xray-server"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fabric>=3.2,<4",
    "python-dotenv>=1.0,<2",
]

[tool.uv]
package = false
